services:
  - type: web
    name: network-monitor
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python web.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0